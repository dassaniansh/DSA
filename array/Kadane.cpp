#include<bits/stdc++.h>
using namespace std;

int kadane(vector<int> arr){
    int maxTemp = 0, maxFinal = 0;
    for(auto num: arr){
        //maxTemp = max(m
    }
}

int main(){
    vector<int> arr = {-2, -3, 4, -1, -2, 1, 5, -3};
    cout<<kadane(arr);
    return 0;
}